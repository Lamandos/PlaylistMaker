<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name="com.example.playlistmaker.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".search.ui.activity.SearchActivity"
            android:theme="@style/Theme.SearchScreen"
            android:exported="true" />
        <activity
            android:name=".MediaActivity"
            android:exported="false" />
        <activity
            android:name=".player.ui.activity.PlayerActivity"
            android:theme="@style/Theme.PlayerScreen"
            android:exported="true" />
        <activity
            android:name=".settings.ui.activity.SettingsActivity"
            android:theme="@style/Theme.SettingsScreen"
            android:exported="true" />
        <activity
            android:name=".main.ui.activity.MainActivity"
            android:theme="@style/Theme.MainScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>